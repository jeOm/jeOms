#ifndef BOARD_H
#define BOARD_H
#include <iostream>
using namespace std;

class board
{
    public:
        board();
        virtual ~board();
        void initialize();
        void render();
        void update(int move,char icon);
        bool checkwin();
        bool gameDraw();
    protected:
    private:
        char box[9];

};

#endif // BOARD_H
